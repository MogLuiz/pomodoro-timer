import * as S from './styles'

export const Header = () => {
  return (
    <S.HeaderContainer>
      <span>Logo</span>
      <nav>
        <a href="#">timer</a>
        <a href="#">history</a>
      </nav>
    </S.HeaderContainer>
  )
}
